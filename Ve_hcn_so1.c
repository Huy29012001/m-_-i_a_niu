#include <stdio.h>
#include <math.h>
#include <conio.h>

int main()
{
    int i, a, b, k;
	scanf("%d", &b);
	scanf("%d", &a);
	if ((a != 0) && (b != 0 ))
	{
		for (i = 1; i <= b; i++)
		{
			for (k = 1; k <= a; k++)
			{
				printf("1");
			}
			printf("\n");

		}
	}
	
}